<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="thanhlongbanh8997.englishforeverybody">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/capture"
        android:label="Smart English"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TypeTrainingActivity"/>
        <activity android:name=".grammar.TenseQuestion"/>
        <activity android:name=".GrammarStartActivity"/>
        <activity android:name=".grammar.PassivoiceQuestion"/>
        <activity android:name=".grammar.GerundAndToVerb"/>
        <activity android:name=".grammar.RelativeClause"/>
        <activity android:name=".EditorVocabularyActivity"/>
        <activity android:name=".HomeActivity"/>
        <activity android:name=".FeedbackActivity"/>
        <activity android:name=".AboutMeActivity"/>
        <activity android:name=".listening.ContentListening"/>

    </application>

</manifest>